Notes
- we use native file dialogs which allow to interact better with the filesystem than Qt file dialogs


TODO
- [don] package a desktop file
- adapt code to using flatpak, which does not have full access to filesystem
	- [done] find a way to select a directory
	- apply checks for directory existence
	- clone dialog checks for directory existence
	- clone task ordering checks for directory existence
	- when selecting a multigit file for cloning, the path given is inside the sandbox
		=> we have code to convert the file to host path but it uses an API not available in 20.04
		=> to be tested on a more recent OS
	- open a dir in tmp and see how Multigit reacts
- sublime merge is not autodtected (because it is installed as a flatpak)
    => add flatak + snap autodetection
    => add flatak + snap launcher
- see if we can create a directory from a native open file dialog instead of regualar file dialog
- default dialog size are too big
- show last commit _+ show modified files is not activatee by default


- clean-up the build dir to reduce the size of the final program


- destkop file should be: org.multigit.Multigit.desktop and should be exported
- icon should be provided with command: rename-icom


- create a metainfo.xml file and install it properly
- export to a flatpak repository
- sign with gpg key